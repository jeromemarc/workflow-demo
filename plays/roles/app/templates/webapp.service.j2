# {{ ansible_managed }}
[Unit]
Description=uWSGI instance to serve Flask app
After=network.target

[Service]
User=root
Group=root
WorkingDirectory={{ app_path }}
Environment="PATH={{ app_path }}/support/bin"
ExecStart={{ app_path }}/support/bin/uwsgi --ini uwsgi.ini

[Install]
WantedBy=multi-user.target

